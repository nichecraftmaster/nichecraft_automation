{
  "name": "nichecraft-automation",
  "version": "1.0.0",
  "scripts": {
    "start": "vercel dev"
  },
  "dependencies": {
    "stripe": "^10.0.0"  // Make sure the latest version of Stripe is used
  }
}
